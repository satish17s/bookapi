{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd gutenberg_api && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn gutenberg_api.gutenberg_api.wsgi:application --bind 0.0.0.0:$PORT",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10,
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
    }
}
